#!/bin/bash

# This script runs the MCP client test

# Install necessary dependencies if not already installed
npm install @modelcontextprotocol/sdk

# Run the MCP client test
node scripts/test-mcp.js
